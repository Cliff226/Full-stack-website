<!DOCTYPE html>
<html lang="en">
    {{ include('header.html.twig') }}
    <body>

        <header class="header">
            <nav>
                {# Include navigation template #}
                {{ include('navigation.html.twig') }}
            </nav>
        </header>
        <main class="home">
            <div class="home">
                <div class="banner">
                    <h1>Welcome to GoalRush {{ userData.name|upper }}</h1>

                    <p>
                        Your ultimate destination for live football scores and news.
                        {% if userData is defined and userData.favorite_league %}
                            Stay up to date with the latest from your favorite league: {{ userData.favorite_league|upper }}!
                        {% else %}
                            Discover scores, news, and updates from all leagues worldwide.
                        {% endif %}
                    </p>
                    <a id="scrolldownlink" class="homebtn btn-primary"  scrolldown href="#intro">Explore</a>
                </div>
            </div>
            <div class="row g-0 intro">
                <div class="col sml-6">
                    <div id="carouselExampleFade" class="carousel slide carousel-fade">
                        <div class="carousel-inner">
                            <div class="carousel-item active">
                                <a class ="link-section-ancor" href="/liveScore.php">
                                    <img class=" d-block link-section-img" src="public/images/kick.jpg" alt="kick image">
                                    <h3>Live Scores</h3>
                                </a>
                            </div>
                            <div class="carousel-item">
                                <a class ="link-section-ancor" href="/blog.php">
                                    <img class="link-section-img" src="public/images/championsball.jpg" alt="ball image">
                                    <h3>Blog</h3>
                                </a>
                            </div>
                            <div class="carousel-item">
                                {% if user %}
                                    <a class ="link-section-ancor" href="/profile.php">
                                        <img class="link-section-img" src="public/images/stadium.jpg" alt="net image">
                                        <h3>Your profile</h3>
                                    </a>
                                {% else %}
                                    <a class ="link-section-ancor" href="/login.php">
                                        <img class="link-section-img" src="public/images/stadium.jpg" alt="net image">
                                        <h3>Login</h3>
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Previous</span>
                        </button>
                        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Next</span>
                        </button>
                    </div>
                </div>
                <div id = "intro" class="col sml-6">
                    {% if user %}
                {# Logged-in content #}
                <div class="text-center p-6 bg-green-100 rounded-xl shadow">
                    <h2 class="text-2xl font-bold mb-3">Welcome Back!</h2>
                    <p class="text-gray-700 mb-5">
                        New posts are live — explore the latest blogs and share your thoughts.
                    </p>
                    <div class="btn-group buttonsindex" role="group" aria-label="Basic mixed styles example">
                        <a href="blog.php" class= "btn btn-primary">Read Blogs</a>
                        <a href="profile.php" class= "btn btn-primary">Your Profile</a>
                    </div>
                </div>
            {% else %}
                {# Guest content #}
                <div class="text-center p-6 bg-gray-100 rounded-xl shadow">
                    <h2 class="text-2xl font-bold mb-3">Unlock Exclusive Content</h2>
                    <p class="text-gray-600 mb-5">
                        <p>"Become part of
                         the GoalRush community.
                         Create an account
                         or log in
                         to access our blogs and 
                         engage with other fans."
                        </p>
                    </p>
                    <div class="btn-group buttonsindex" role="group" aria-label="Basic mixed styles example">
                        <a href="register.php" class="btn btn-primary">Register</a>
                        <a href="login.php" class="btn btn-primary">Log In</a>
                    </div>
                </div>
            {% endif %}
        </main>
        <footer class="footer mt-4">
            <p>Copyright © 2025</p>
        </footer>
        {% include 'searchScript.html.twig' %}
    </body>
</html>